// need to be run from a browser

const firstName = prompt("Enter your first name: ");
const lastName = prompt("Enter your last name: ");

const string = `Hello, ${firstName} ${lastName}`
alert(string);
console.log(string);